// src/App.jsx
import HarryPotterCTF from './HarryPotterCTF';
import './App.css'; // Make sure you keep the CSS import if present

function App() {
  return (
    // The HarryPotterCTF component already has the full styling
    // and structure, so we just render it directly.
    <HarryPotterCTF /> 
  );
}

export default App;